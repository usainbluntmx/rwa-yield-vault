export default function Vault() {
    return (
        <div>
            <h2>Vault</h2>

            {/* TODO tu código actual del vault */}
            {/* botones deposit / withdraw / balance / etc */}
        </div>
    )
}
